export type Member = {
    id: string;
    firstName: string;
    lastName: string;
    dateOfBirth: string;
    gender: string;
    imageUrl?: string;
    created: string;
    lastActive: string;
    description?: string;
    clientId: string;
    userId: string;
    role?: string;
}